apiVersion: v1
kind: Service
metadata:
  name: sgl-middleware
  namespace: default
spec:
  selector:
    app: sgl-middleware
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: LoadBalancer
